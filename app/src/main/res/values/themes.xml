<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.PowerHome" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your light theme here. -->
    </style>
    <style name="Theme.PowerHome" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Couleurs claires par défaut -->
        <item name="colorPrimary">#C1E1C1</item>
        <item name="colorPrimaryDark">#5F9EA0</item>
        <item name="colorAccent">@color/teal_200</item>
        <item name="android:windowBackground">@drawable/gradient_background</item>
        <item name="android:textColorPrimary">@color/black</item>
        <item name="cardViewStyle">@style/CardView.Light</item>
    </style>
    <style name="Theme.PowerHome.Dark" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Thème sombre -->
        <item name="colorPrimary">#478778</item>
        <item name="colorPrimaryDark">#097969</item>
        <item name="colorOnPrimary">@color/black</item>
        <item name="android:windowBackground">@drawable/gradient_background_dm</item>
        <item name="android:textColorPrimary">@color/white</item>
        <item name="cardViewStyle">@style/CardView.Dark</item>
    </style>
    <style name="CardView.Light" parent="CardView">
        <item name="cardBackgroundColor">@color/white</item>
        <item name="cardElevation">4dp</item>
    </style>
    <style name="CardView.Dark" parent="CardView">
        <item name="cardBackgroundColor">#5F8575</item>
        <item name="cardElevation">4dp</item>
    </style>
</resources>